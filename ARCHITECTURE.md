# TECHHUB Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         TECHHUB E-COMMERCE                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐          ┌──────────────────────────┐
│      FRONTEND            │          │       BACKEND            │
│   (Browser/Client)       │          │  (Node.js/Express)       │
├──────────────────────────┤          ├──────────────────────────┤
│ • index.html             │◄────────►│ • Express Server         │
│ • login.html             │  HTTP/   │ • MongoDB Driver         │
│ • register.html          │  JSON    │ • Authentication         │
│ • admin.html             │          │ • Product Management     │
│ • script.js              │          │ • Order Processing       │
│ • auth.js                │          └──────────────────────────┘
│ • style.css              │
└──────────────────────────┘          ┌──────────────────────────┐
       ↓                               │     MONGODB DATABASE     │
   [LocalStorage]           ◄────────►│  (Cloud/Local Instance)  │
   (JWT Token)                        ├──────────────────────────┤
   (User Data)                        │ • Users Collection       │
   (Cart Items)                       │ • Products Collection    │
                                      │ • Orders Collection      │
                                      └──────────────────────────┘
```

---

## User Authentication Flow

```
                    ┌─────────────────────┐
                    │    START: VISIT      │
                    │   TECHHUB WEBSITE    │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │  Logged In Check    │
                    └──┬─────────────────┬┘
                       │                 │
                      YES               NO
                       │                 │
                       │         ┌───────▼──────┐
                       │         │  Show Login  │
                       │         │ & Register   │
                       │         │  Buttons     │
                       │         └───────┬──────┘
                       │                 │
            ┌──────────▼────────────────▼──────────┐
            │  User Clicks Register or Login       │
            └───┬─────────────────────────────┬───┘
                │                             │
            Register                      Login
                │                             │
        ┌───────▼────────┐           ┌───────▼────────┐
        │ Fill Form:     │           │  Enter Creds:  │
        │ • Username     │           │ • Username     │
        │ • Email        │           │ • Password     │
        │ • Password     │           └───────┬────────┘
        │ • Role (U/A)   │                   │
        └───────┬────────┘           ┌───────▼────────┐
                │                    │ Verify Against │
        ┌───────▼────────┐           │   Database     │
        │  Validate Form │           └───┬────────┬──┘
        └───┬────────┬──┘                │        │
            │        │              Valid    Invalid
            │        │                │        │
           OK    Invalid          ┌───▼───┐  │
            │        │            │       │  │
        ┌───▼───┐  ┌──▼───┐   Generate  Error
        │Create │  │Error │   JWT Token Message
        │User   │  │Alert │      │
        │Hash   │  └──────┘      │
        │Pass   │            ┌───▼────────┐
        │Save   │            │   Store    │
        │to DB  │            │ Token in   │
        └───┬───┘            │LocalStorage│
            │                └───┬────────┘
        ┌───▼────────┐           │
        │ Generate   │           │
        │ JWT Token  │       ┌───▼────────┐
        └───┬────────┘       │ Redirect to│
            │                │   Home or  │
        ┌───▼────────┐       │   Admin    │
        │   Store    │       └────────────┘
        │   Token    │
        └───┬────────┘
            │
        ┌───▼─────────────────┐
        │  User is LOGGED IN  │
        │  Can shop & checkout│
        └─────────────────────┘
```

---

## Shopping Cart & Checkout Flow

```
┌──────────────────────────┐
│  Browse Products         │
│  (Public Page)           │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Click "Add to Cart"      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Check Authentication              │
└──┬───────────────────────────┬───┘
   │                           │
Logged In                  Not Logged In
   │                           │
   ▼                           ▼
┌─────────────┐        ┌──────────────────┐
│  Add Item   │        │ Alert: Login     │
│  to Cart    │        │ Required         │
│  (Local)    │        │ Redirect to      │
└────┬────────┘        │ login.html       │
     │                 └──────────────────┘
     ▼
┌──────────────────────┐
│ Click Cart Button    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Display Cart Offcanvas       │
│ • Items with qty/price       │
│ • Update qty buttons (+/-)   │
│ • Remove buttons             │
│ • Total calculation          │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Check Authentication Again   │
└──┬────────────────┬───────┬──┘
   │                │       │
Logged In        Not Logged In
   │                │
   ▼                ▼
┌──────────┐   ┌──────────────┐
│ Continue │   │ Show Login   │
│ Checkout │   │ Alert        │
└────┬─────┘   └──────────────┘
     │
     ▼
┌──────────────────────────┐
│ Fill Checkout Form:      │
│ • Full Name              │
│ • Email                  │
│ • Address                │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Click "Place Order"      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Send to Backend API:     │
│ POST /checkout           │
│ (with JWT Token)         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Backend Validates:       │
│ • Verify JWT Token       │
│ • Check Cart Items       │
│ • Save Order to MongoDB  │
└──┬──────────────┬────────┘
   │              │
Success        Error
   │              │
   ▼              ▼
┌─────────┐   ┌──────────┐
│ Return  │   │ Return   │
│ OrderID │   │ Error    │
└────┬────┘   └────┬─────┘
     │             │
     ▼             ▼
┌──────────────┐ ┌──────────┐
│ Clear Cart   │ │ Show     │
│ Show Success │ │ Error    │
│ with Order # │ │ Message  │
└──────────────┘ └──────────┘
```

---

## Admin Dashboard Flow

```
┌──────────────────────┐
│ Login Page           │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Enter Admin Credentials      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Backend Verifies:            │
│ • Valid username/password    │
│ • Role = "admin"             │
└──┬───────────────────────┬──┘
   │                       │
Success                 Failure
   │                       │
   ▼                       ▼
┌─────────────┐     ┌──────────────┐
│ Generate    │     │ Deny Access  │
│ JWT Token   │     │ Redirect to  │
│ (admin)     │     │ Login        │
└────┬────────┘     └──────────────┘
     │
     ▼
┌──────────────────────────────┐
│ Redirect to admin.html       │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Admin Dashboard Loaded            │
│ • Navbar shows "Admin" button     │
│ • Display Products & Orders tabs  │
└──┬────────────┬──────────────┬───┘
   │            │              │
Products    Orders         Settings
   │            │
   ▼            ▼
┌────────┐  ┌────────┐
│Products│  │Orders  │
│Tab     │  │Tab     │
│        │  │        │
│• Create│  │• View  │
│• Read  │  │• Update│
│• Update│  │  Status│
│• Delete│  │• Delete│
└────────┘  └────────┘
```

---

## Product CRUD Operations (Admin)

```
PRODUCTS TAB
    │
    ├─► ADD PRODUCT
    │   └─► Click "Add New Product"
    │   └─► Modal Opens
    │   └─► Fill Form:
    │       • Name
    │       • Description
    │       • Price
    │       • Stock
    │       • Image URL
    │   └─► Click "Save Product"
    │   └─► POST /api/products (with admin JWT)
    │   └─► Success: Product added to DB & grid
    │
    ├─► READ PRODUCT
    │   └─► Products displayed in grid
    │   └─► GET /api/products (public)
    │   └─► Shows all products
    │
    ├─► UPDATE PRODUCT
    │   └─► Click "Edit" button on product
    │   └─► Modal opens with current data
    │   └─► Edit fields
    │   └─► Click "Save Product"
    │   └─► PUT /api/products/:id (with admin JWT)
    │   └─► Success: Product updated
    │
    └─► DELETE PRODUCT
        └─► Click "Delete" button
        └─► Confirmation dialog
        └─► Confirm deletion
        └─► DELETE /api/products/:id (with admin JWT)
        └─► Success: Product removed from grid
```

---

## Order Management (Admin)

```
ORDERS TAB
    │
    ├─► VIEW ORDERS
    │   └─► GET /api/orders (admin only)
    │   └─► Display in table:
    │       • Order ID
    │       • Customer Name
    │       • Email
    │       • Total
    │       • Status
    │       • Date
    │
    ├─► UPDATE STATUS
    │   └─► Find order in table
    │   └─► Click status dropdown
    │   └─► Select new status:
    │       • Pending
    │       • Processing
    │       • Shipped
    │       • Delivered
    │       • Cancelled
    │   └─► PUT /api/orders/:id (with admin JWT)
    │   └─► Table updates automatically
    │
    └─► VIEW DETAILS
        └─► Click "View" button
        └─► Show order details:
            • Items in order
            • Customer info
            • Order date
            • Status history
```

---

## Database Schema & Relationships

```
┌─────────────────────────────────────┐
│        USERS COLLECTION             │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ username: String (unique)           │
│ email: String (unique)              │
│ password: String (hashed)           │
│ role: String (user/admin)           │
│ createdAt: Date                     │
└─────────────────────────────────────┘
         │                △
         │                │
         │ Has Many       │ Ref
         ▼                │
┌─────────────────────────────────────┐
│        ORDERS COLLECTION            │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ userId: ObjectId ────────────────┐  │
│ customer: {                       │  │
│   name: String                    │  │
│   email: String                   │  │
│   address: String                 │  │
│ }                                 │  │
│ items: [                          │  │
│   {                               │  │
│     productId: ObjectId ──────┐   │  │
│     name: String              │   │  │
│     price: Number             │   │  │
│     qty: Number               │   │  │
│   }                           │   │  │
│ ]                             │   │  │
│ total: Number                 │   │  │
│ status: String                │   │  │
│ createdAt: Date               │   │  │
└─────────────────────────────────────┘
         △                        │
         │ Ref                    │ Ref
         └────────────────────────┘
         │ Has Many
┌─────────────────────────────────────┐
│      PRODUCTS COLLECTION            │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ name: String                        │
│ desc: String                        │
│ price: Number                       │
│ stock: Number                       │
│ image: String (URL)                 │
│ createdAt: Date                     │
└─────────────────────────────────────┘
```

---

## Request/Response Flow Example

### Checkout Request Flow
```
FRONTEND (script.js)
    │
    ├─ User clicks "Place Order"
    │
    ├─ Check isAuthenticated()
    │
    ├─ Call createOrder(customerInfo, items, total)
    │
    ├─ authenticatedFetch() with JWT Token
    │
    └─► HTTP Request
        POST /checkout
        Authorization: Bearer eyJhbGc...
        Content-Type: application/json
        {
          "customer": {
            "name": "John Doe",
            "email": "john@example.com",
            "address": "123 Main St"
          },
          "items": [
            {
              "id": 1,
              "name": "Laptop",
              "price": 75988.50,
              "qty": 1
            }
          ],
          "total": 75988.50
        }
            │
            ▼
BACKEND (server.js)
    │
    ├─ Receive POST request
    │
    ├─ verifyToken() middleware
    │   └─ Verify JWT
    │   └─ Extract userId
    │
    ├─ Validate request body
    │   └─ Check items.length > 0
    │   └─ Check customer info
    │
    ├─ Create Order document
    │   └─ userId: req.user.id
    │   └─ customer: {...}
    │   └─ items: [...]
    │   └─ total: ...
    │
    ├─ Save to MongoDB
    │
    └─► HTTP Response (201)
        {
          "success": true,
          "orderId": "507f1f77bcf86cd799439011",
          "message": "Order created successfully"
        }
            │
            ▼
FRONTEND (auth.js / script.js)
    │
    ├─ Receive response
    │
    ├─ Check success flag
    │
    ├─ Clear localStorage cart
    │
    ├─ Reset form
    │
    ├─ Close offcanvas
    │
    └─► Show success alert with Order ID
```

---

## Security Flow (Authentication)

```
┌─────────────────────────────────────────┐
│ LOGIN/REGISTER REQUEST                  │
└──────────────────┬──────────────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Send Credentials     │
        │ Username + Password  │
        │ (HTTPS/TLS)          │
        └──────────┬───────────┘
                   │
                   ▼ (to Backend)
        ┌──────────────────────────────┐
        │ Backend Receives Credentials │
        └──────────┬───────────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Find User in MongoDB     │
        └──┬──────────────────┬───┘
           │                  │
        Found            Not Found
           │                  │
           ▼                  ▼
        ┌────────────┐   ┌──────────────┐
        │ Compare    │   │ Return Error │
        │ Password:  │   │ 401 Invalid  │
        │ bcrypt.    │   │ Credentials  │
        │ compare()  │   └──────────────┘
        └────┬───┬──┘
             │   │
           Match Mismatch
             │   │
             ▼   ▼
        ┌────┐ ┌────────────┐
        │ ✓  │ │ ✗ Return  │
        │    │ │ Error 401 │
        └─┬──┘ └────────────┘
          │
          ▼
┌──────────────────────────┐
│ Generate JWT Token       │
│ Header: { alg: "HS256" } │
│ Payload: {               │
│   id: user._id,          │
│   username: user.name,   │
│   role: user.role,       │
│   exp: +7days            │
│ }                        │
│ Secret: JWT_SECRET       │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Return JWT to Frontend   │
│ HTTP 200 Response        │
│ {                        │
│   token: "eyJhbGc...",   │
│   user: {...}            │
│ }                        │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Frontend Stores Token    │
│ localStorage.setItem(    │
│   'authToken', token     │
│ )                        │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Protected Request (Cart/Order)   │
│ Header: {                        │
│   Authorization:                 │
│   "Bearer eyJhbGc..."            │
│ }                                │
└────────┬───────────────────────┬─┘
         │                       │
         ▼ (to Backend)      (From Frontend)
┌──────────────────────────┐
│ Backend receives request │
│ with JWT in header       │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ verifyToken middleware   │
│ jwt.verify(token,        │
│   JWT_SECRET)            │
└──┬──────────┬────────────┘
   │          │
Valid      Invalid
   │          │
   ▼          ▼
 ✓ Proceed  ✗ Return 401
          Unauthorized
   │
   ▼
┌──────────────────────┐
│ Check Admin Role     │
│ (if needed)          │
└──┬──────────┬───────┘
   │          │
Admin      Not Admin
   │          │
   ▼          ▼
 ✓ Allow  ✗ Return 403
         Forbidden
   │
   ▼
┌──────────────────────┐
│ Process Request      │
│ Execute API Logic    │
└──────────────────────┘
```

---

## File Structure

```
yawa/
├── 📄 index.html                    (Home page)
├── 📄 login.html                    (Login)
├── 📄 register.html                 (Registration)
├── 📄 admin.html                    (Admin dashboard)
├── 📄 about.html                    (About page)
├── 🎨 style.css                     (Styling)
├── 📜 script.js                     (Frontend logic)
├── 📜 auth.js                       (Auth helper functions)
├── 📦 package.json                  (Dependencies)
├── 📖 README.md                     (Documentation)
├── 📖 TESTING.md                    (Test scenarios)
├── 📖 IMPLEMENTATION_SUMMARY.md      (This summary)
├── 📖 ARCHITECTURE.md                (Architecture diagrams)
├── 📂 backend/
│   └── 📜 server.js                 (Express backend)
├── 📂 assets/
│   ├── 🖼️ laptop.svg
│   ├── 🖼️ headphones.svg
│   └── ... (other product SVGs)
└── 📂 node_modules/                 (Installed packages)
```

---

This document provides visual representations of all the major flows in the TECHHUB system!
